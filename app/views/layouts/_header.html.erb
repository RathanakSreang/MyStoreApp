<nav class="navbar navbar-default navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">      
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#user_profile">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>        
      </button>
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#search-bar">        
        <i class="glyphicon glyphicon-search"></i>      
      </button>
      <%#= link_to "My Tob", root_url(subdomain: false), class: "only-xs navbar-brand" %>
    </div>
    <div class="row">
      <div class="col-sm-12 user-bar">
      <div class="container">
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="user_profile">
          <ul class="nav navbar-nav navbar-right">
            <% if user_signed_in? %>
              <li class="dropdown">
                <%= link_to "#", class:"dropdown-toggle", id:"user-header", 
                          'aria-expanded' =>'false', data: {toggle: "dropdown"} do %>
                  <span class="user-name">
                    <b><%= current_user.name.capitalize %></b>
                    <span class="caret"></span>
                  </span>                  
                <% end %>
                <ul id="login-dp" class="dropdown-menu">    
                  <li>
                    <%= link_to current_user do%>
                      <i class="fa fa-user"> </i>  Profile
                    <%end%>
                  </li>
                  <li class="divider"></li>
                  <li>
                    <% if current_user.store %>
                      <%= link_to admin_url(subdomain: current_user.store.sub_domains.first.name) do %>
                        <i class="fa fa-cog"></i>  My Tob
                      <% end %>
                    <% else %>
                      <%= link_to  new_store_path do %>
                        <i class="fa fa-cog"></i>  Create Tob
                      <% end %>
                    <% end %>
                  </li>
                  <li class="divider"></li>
                  <li>    
                  <%= link_to edit_user_registration_path do%>
                    <i class="fa fa-cog"></i>  Sitting
                  <%end%>
                  </li>
                  <li class="divider"></li>
                  <li><%= link_to sign_out_path do%>
                    <center><i class="fa fa-sign-out fa-lg"></i>  Sign Out</center>      
                  <%end%>    
                  </li>
                  <br/>
                </ul>
              </li>
            <% else %>              
              <li class="dropdown">
                <a href="#" class="dropdown-toggle reset" data-toggle="dropdown"><b>Sign in</b> <span class="caret"></span></a>
                <ul id="login-dp" class="dropdown-menu">
                  <li>
                    <div class="row">
                      <div class="col-md-12">
                        <%= render "devise/sessions/sign_in" %>
                      </div>
                    </div>
                  </li>
                </ul>
              </li>
            <% end %>      
          </ul>
        </div><!-- /.navbar-collapse -->
      </div>
      </div>
      <div class="col-sm-12 search">
        <div class="container">
        <div class="row">
          <div class="col-md-2">
            
          </div>
          <div class="col-md-8">
            <div class="collapse navbar-collapse" id="search-bar">
              <ul class="nav navbar-nav">
                <%= form_tag stores_path,class: 'navbar-form', role: 'search', :method => :get do %>       
                <div class="input-group">
                  <%= text_field_tag :search, params[:search],
                  {class:"form-control", 
                    placeholder:"Search Shop"} %>
                    <div class="input-group-btn">              
                      <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>              
                    </div>            
                </div>        
              <%end %>
              </ul>
            </div>  
          </div>
          <div class="col-md-2">
            
          </div>
        </div>
        </div>
      </div>
    </div>  
  </div>
</nav>
