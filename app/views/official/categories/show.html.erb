<div class="row">
  <div class="col-md-6 col-md-offset-3 well">
    <legend>Category</legend>
    <%= @category.name %>    
    <br/>
    <%= link_to "Edit", edit_official_category_path(@category), class: "btn btn-primary" %>    
    <%= link_to "Delete", official_category_path(@category), method: :delete, data: {confirm: "Are you sure?"}, class: "btn btn-danger" %>
  </div>
</div>
